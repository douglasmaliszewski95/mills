import { ImageCMS } from "@/types";

export interface TransportContent {
  banner: ImageCMS;
  firstAbout: ImageCMS;
  talkToSpecialist: ImageCMS;
  differentials: Item[];
  constructionCards: ImageCMS[];
}
