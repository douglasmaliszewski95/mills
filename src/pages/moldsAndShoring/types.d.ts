import { ImageCMS } from "@/types";

export interface MoldsContent {
  banner: ImageCMS;
  firstAbout: ImageCMS;
  secondAbout: ImageCMS;
  molds: ImageCMS;
  shoring: ImageCMS;
  specialSystems: ImaceCMS;
  segments: ImageCMS[];
  differentials: Item[];
  constructionCards: ImageCMS[];
}
